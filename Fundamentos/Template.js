const nome = "Thiago"
const concatenação = "Olá, " + nome + "!"
const template = `string text ${nome} string text`
console.log(template)

const up = texto => texto.toUpperCase()
console.log(`Ei... ${up("cuidado")}`)
